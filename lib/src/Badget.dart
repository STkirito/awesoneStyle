import 'package:flutter/widgets.dart';

//abstract class BadgetWidgets extends StatelessWidget {}
abstract class BadgetWidgets extends StatefulWidget {
  final Key? key;
  BadgetWidgets({this.key}) : super(key: key);
}
